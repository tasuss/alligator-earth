import {{=it.nameTitle}} from "./fce/{{=it.name}}.interface";
import {{=it.nameTitle}}Bit from "./fce/{{=it.name}}.interface";

export class {{=it.nameTitle}}Model implements {{=it.nameTitle}} {
 idx:string = '{{=it.pivot}}';
 //controlBitList: ControlBit[] = [];
 //controlBits: any = {};
}


