export const initBitmap = (cpy: BitmapModel, bal:BitmapBit, ste: State) => {
 debugger
 return cpy;
};

export const updateBitmap = (cpy: BitmapModel, bal:BitmapBit, ste: State) => {
 return cpy;
};


import { BitmapModel } from "../bitmap.model";
import BitmapBit from "../fce/bitmap.bit";
import State from "../../99.core/state";